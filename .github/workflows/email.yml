name: Portfolio Action

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: 22f3002965@ds.study.iitm.ac.in
        run: echo "Portfolio workflow running"
